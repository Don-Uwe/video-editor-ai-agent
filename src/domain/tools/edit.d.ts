export declare function cutClip(source: string, start: number, end: number, output: string): string;
export declare function sequenceClips(clips: string[], output: string): string;
export declare function addMusic(video: string, music: string, output: string, musicVolume?: number): string;
